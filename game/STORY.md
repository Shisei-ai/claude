# シナリオ執筆ガイド（VN会話劇）

『鋼鉄戦線』のストーリーは、各章の **開幕（intro）** と **終幕（outro）** で再生される
**VN（ビジュアルノベル）風の会話劇** です。立ち絵＋名前プレート＋台詞ボックスで、
複数キャラが順に喋り、表情・立ち位置・演出を指定できます。

データは全て **`js/data.js`** に集約しています。台本を書く／渡すだけでOKです。

---

## 1. 原稿の渡し方（自由形式でOK）

台本は**普通の脚本の形**で書いてくれれば、こちらが下記のビート配列に整形して流し込みます。
例：

```
【第一章 開幕】

ナレーション：それが、【オールト】の実際であった。
管理者：さて、入館が済んだ訳だが……
ケイオス（笑み）：長旅、お疲れ様でした。私はサポートAI、ケイオスです。
管理者（驚愕）：は、7割！？！？　喪失！？！？  ＜画面を揺らす＞
```

- 「キャラ名：台詞」の形式で行を並べるだけでOK。
- 括弧で **表情**（驚き・笑顔 等）、`＜＞`で **演出**（揺らす・フラッシュ 等）を書き添えてくれれば反映します。
- 地の文は「ナレーション：」または話者なしで。
- 章ごとに「開幕／終幕」を分けてください。

---

## 2. 登場人物（`G.CHARACTERS`）

`js/data.js` 内の `G.CHARACTERS` がキャラ登録簿です。自由に追加・改名できます。

```js
G.CHARACTERS = {
  narration: { name: '', color: '#cdd8e6', side: 'center' },        // 地の文（消さないで）
  oort:   { name: '【オールト】', color: '#9fb4c8', side: 'center', icon: '📡' },          // 基地の自動音声
  kchaos: { name: 'ケイオス',     color: '#37d0ff', side: 'left',  icon: '🤖', hud: true, img: {} }, // サポートAI（ボディ起動前。HUD表示）
  kchaosb:{ name: 'ケイオス',     color: '#37d0ff', side: 'left',  icon: '👩', img: {} },  // ボディを得たケイオス（第三章〜。全身立ち絵）
  master: { name: '管理者',       color: '#ffd24a', side: 'right', icon: '🧑', img: {} },  // 主人公（私／マスター）
  swarm:  { name: '？？？',       color: '#ff5d6c', side: 'right', icon: '☣', img: {} },   // 謎の襲撃者
};
```

| フィールド | 意味 |
|-----------|------|
| `name`  | 名前プレートに出る表示名 |
| `color` | 名前の色（テーマカラー） |
| `side`  | 既定の立ち位置 `left` / `right` / `center`（center=立ち絵なし） |
| `icon`  | 立ち絵PNG未配置時のプレースホルダ用の絵文字 |
| `hud`   | `true` で立ち絵を小さなHUD風（画面上方）に。肉体を持たないAIなどに（任意） |
| `img`   | 表情ごとの立ち絵PNGパス（`portraits/` に置く。下記参照） |

### 立ち絵PNGの割り当て
画像は `portraits/` に置き、`img` に表情名→パスでマッピングします（仕様は `portraits/README.md`）。

```js
kchaos: {
  name: 'ケイオス', color: '#37d0ff', side: 'left', icon: '🤖',
  img: { neutral: 'portraits/chaos.png', smile: 'portraits/chaos_smile.png' },
},
```

PNG未配置の間は自動でプレースホルダ表示になるので、**台本だけ先に進められます**。

---

## 3. ビート（台詞1行）の書式

`intro` / `outro` は次の2形式どちらでも取れます。

- **文字列** … 地の文として表示（空行で段落＝ページ送り）。手軽。
- **ビート配列** … 立ち絵つきの会話劇。表現力が高い。

ビート1個＝台詞1行のオブジェクト：

```js
{ who: 'master', expr: 'shock', pos: 'right', fx: 'shake', text: 'は、7割！？！？　喪失！？！？' }
```

| キー | 必須 | 説明 |
|------|------|------|
| `who`  | 〇 | 話者のキャラID（`G.CHARACTERS` のキー）。`narration` で地の文 |
| `text` | △ | 台詞。省略すると「演出だけのビート」になり自動で次へ進む |
| `expr` |   | 表情名（`img` のキー。例 `neutral` / `smile` / `shock`）。立ち絵を差し替える |
| `pos`  |   | この台詞での立ち位置 `left` / `right`（省略時はキャラの `side`） |
| `fx`   |   | 画面演出：`shake`（揺れ）/ `flash`（発光）|
| `flashColor` | | `fx:'flash'` の色（例 `'rgba(255,40,40,.6)'`）|
| `bg`   |   | 背景の差し替え（CSSの background 値。例 `'radial-gradient(...)'`）|
| `cutin`|   | バナー型カットイン：`{ jp:'警告', en:'ALERT', cls:'boss', dur:2000 }` |

### 演出だけのビート（text省略）
場面転換・効果だけ入れたい時は `text` を省略すると、約0.36秒後に自動で次のビートへ進みます。

```js
{ bg: 'radial-gradient(120% 120% at 50% 30%, #2a0d12, #06080e 78%)' },
{ fx: 'flash', flashColor: 'rgba(255,40,40,.55)' },
{ who: 'swarm', expr: 'neutral', text: '……喰ラウ……' },
```

---

## 4. 完成サンプル（第一章 開幕）

実際に第一章 `intro` で使っている抜粋（全文は `js/data.js` 参照）：

```js
intro: [
  { who: 'oort', text: 'ようこそ、人類文明の最前線たる研究基地【オールト】へ。\n当館の全ては貴方を歓迎し、服従します。' },
  { who: 'narration', text: 'それが、【オールト】の実際であった。' },
  { who: 'master', expr: 'neutral', text: 'さて、入館が済んだ訳だが……' },
  { who: 'kchaos', expr: 'smile', text: '長旅、お疲れ様でした。私はサポートAI、【C.H.A.O.S】です。ケイオス、とお呼びください。' },
  { who: 'master', expr: 'shock', fx: 'shake', text: 'そうか、7割が……は、7割！？！？　喪失！？！？' },
  { who: 'narration', fx: 'flash', flashColor: 'rgba(255,40,40,.55)',
    cutin: { jp: '襲撃', en: 'ENEMY RAID', cls: 'boss' }, text: 'けたたましい警報が鳴り響き、赤色灯が明滅する。' },
  { who: 'master', expr: 'fired', fx: 'shake', text: '……くそっ！　やってやるよ！' },
],
```

---

## 5. 戦闘の構成（複数戦闘・幕間・鉱区解放）

1つの章は **開幕 → 戦闘（waves）→ 終幕** の流れですが、**1章に戦闘を複数置けます**。
各戦闘（wave）の合間に **幕間（interlude）会話**を挟んだり、**戦闘の勝利で鉱区を解放**したりできます。
これにより「第二鉱区解放戦に勝つ → 銅が解放 → 会話 → 新種との戦闘 → 真相」のような構成が組めます。

戦闘は `w(敵編成, 報酬, オプション)` で定義します。オプション（任意）：

| キー | 説明 |
|------|------|
| `grant` | この戦闘に**勝つと**解放されるもの。`{ zones:[鉱区index], modules:数 }`（鉱区indexは Ⅰ=0 … Ⅵ=5） |
| `interlude` | この戦闘の**後に再生される会話**（ビート配列）。次の戦闘の前に挟まる |

例（第二章の第二鉱区解放戦）：

```js
w([{ type:'swarmling', count:10, delay:1, gap:0.9 }], { ore:120, research:20 },
  { grant: { zones:[1] },           // 勝利で鉱区Ⅱ（銅）を解放
    interlude: [                     // 勝利後に流れる会話
      { who:'master', expr:'neutral', text:'ふう、銅が供給され始めたか。' },
      { who:'kchaos', expr:'neutral', text:'打てる手は打っておく必要があるでしょう。' },
    ] }),
```

> 台本に「(戦闘クエスト「○○解放戦」…)」「(戦闘。敵に△△が追加)」のような戦闘の区切りを書いておけば、
> こちらで戦闘・幕間・解放のどれに当たるかを判断して組み込みます。

---

## 6. 再生のされ方・操作

- **開幕** … 章カットイン（章番号＋脅威キャラ）→ そのまま会話劇へ。
- **幕間** … 戦闘に勝つと、次の戦闘の前に会話劇が入る。
- **終幕** … 会話劇 → 章に「決断（choice）」があればモーダルへ。
- プレイヤー操作：**クリック/タップで送り**（タイプ中なら一気に全文表示）、右上 **スキップ** で会話を即終了。

---

## 7. まとめ：あなたの作業

1. **台本を書く**（第2節の自由形式でOK。表情・演出は括弧で添えるだけ）。
2. （任意）**立ち絵PNG**を用意して `portraits/` に置く。
3. こちらに渡す → ビート配列へ整形し、`data.js` の各章 `intro`/`outro` に流し込み、
   キャラ登録と画像パスを設定します。

台本だけでも、立ち絵だけでも、片方ずつ進められます。お気軽にどうぞ。
