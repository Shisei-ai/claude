{
  "name": "dark-chronicle-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dark Chronicle — HD-2D風ダークファンタジー・ローグライクRPG (ブラウザ版)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "playwright-core": "^1.61.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
