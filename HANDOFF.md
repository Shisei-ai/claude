# 集成工業プランナー 再構成の引き継ぎ

『アークナイツ：エンドフィールド』の集成工業を、ブラウザ上で厳密に設計・検証するツール。
現在は単一HTMLの試作（約171KB／JS 2,878行）で動いている。これを保守できる構成に作り直す。

このファイルは Claude Code の新規セッションにそのまま渡すための仕様書。
参照実装として `aic-planner.html` を同梱している。**まずこれを読んでから着手すること。**

---

## 0. このツールの目的

> 実際のゲーム内で目的生産物を生産するラインの**最小化と最大効率化**を、
> 手軽かつ厳密に検討する。より省スペース・高効率な工業設計を実現する。

「なんとなく置けるツール」ではない。**台数・面積・電力・搬送本数を数値で詰める**のが役割。

---

## 1. 作業の進め方

```
git checkout -b rebuild/browser-tool
```

- 参照実装は**動く仕様書**として扱う。挙動が食い違ったら参照実装が正しい。
- ただし後述の「既知の未解決課題」は再現しなくてよい（直してよい）。
- §7 の回帰ケースを**移植の合否判定**に使う。数値が再現できたら移植成功。
- 一気に全部書き直さない。§6 の順で、各段階で回帰を通しながら進める。

---

## 2. 目標アーキテクチャ

現行の問題は、単一ファイルにデータ・計算・描画・UIが同居していて、
計算ロジックを単体でテストできないこと。分離が最優先。

```
src/
  domain/            純粋関数のみ。DOM を一切参照しない
    types.ts         データ型（§3）
    constants.ts     ゲーム定数（§4）
    geometry.ts      footprint / ポート幾何 / 回転 / 占有図
    routing.ts       経路探索（ダイクストラ＋ブリッジ）
    solver.ts        稼働率・流量・電力・倉庫収支の収束計算
    planner.ts       目標からの逆算・端数最適化
    layout/
      tiers.ts       段の割り当てと重心法による並び替え
      autoLayout.ts  1本のラインとして配置
      blocks.ts      最小整数ユニットを敷き詰める配置
    coverage.ts      網羅状況・構造点検
  data/
    seed.ts          初期データ（品目68・設備40・レシピ50・エリア15）
  ui/                React または素の TS。domain を呼ぶだけにする
    Board.tsx        canvas 描画と入力
    panels/          パレット／検査／効率／診断／品目バランス
    modals/          データ編集／生産目標／採取ゾーン／保存読込／使い方
  store/             状態管理。エリア切り替えとダーティ管理
test/
  solver.test.ts     §7 の回帰ケース
  routing.test.ts    経路探索の単体テスト
  planner.test.ts    逆算と端数最適化
```

推奨: **Vite + TypeScript + Vitest**。React は任意（canvas 主体なので素の TS でも成立する）。
配布は `vite build` の静的ファイル。単一HTMLで配りたい場合は `vite-plugin-singlefile` を検討。

### 守るべき境界

- `domain/` は `window` / `document` / `canvas` を参照しない。Node で単体テストできること。
- 描画は状態を持たない。`draw(ctx, state, camera)` の形にする。
- 現行は `D.nodes` などのグローバル可変状態に全体が依存している。**ここを最初に断ち切る。**

---

## 3. データモデル

現行の構造をそのまま型にしたもの。JSON の保存形式でもある（後方互換を保つこと）。

```ts
type Phase = 'solid' | 'liquid' | 'gas';   // 固体はベルト、液体・ガスはパイプ
type Conf  = 'ok' | 'part' | 'guess';      // 情報の確度（§5）

interface Item {
  id: string; name: string;
  cat?: string;        // 分類（鉱物／植物／粉末／部品／液体…）自由入力
  color: string;       // 盤面での色
  phase: Phase;
  conf?: Conf;
}

interface Port { t: 'in' | 'out'; s: 0|1|2|3; o: number }  // s: 0=北 1=東 2=南 3=西 / o: 辺に沿った位置

interface Facility {
  id: string; name: string;
  kind: 'field'|'source'|'process'|'generator'|'power'|'logistics'|'sink';
  w: number; h: number;
  power: number;       // 消費電力
  gen: number;         // 発電量（generator のみ）
  ports?: Port[];      // 未指定なら nIn/nOut から自動生成
  nIn?: number; nOut?: number;
  tap?: boolean;       // 倉庫搬出口。レシピではなく「引き出す品目」を持つ
  field?: boolean;     // フィールドの採取設備。工業エリアの盤面には置かない
  split?: boolean;     // 分流器（合流器と区別）
  phase?: 'liquid'|'gas'|'fluid';  // パイプ系の物流・貯蔵設備
  env?: boolean;       // ガス散布機など環境設備
  max?: number;        // 設置上限（天有洪炉は12）
  cap?: number;        // 協約容量の消費（未指定なら §5 の推定ルール）
  takes?: string[];    // 採取設備が採れる品目
}

interface Recipe {
  id: string; name: string; conf: Conf;
  fac: string;         // 設備 id
  sec: number;         // 1周期の秒数
  in:  {item:string; qty:number}[];
  out: {item:string; qty:number}[];   // out[0] が主産物。2番目以降は副産物（§5）
}

interface Node {                     // 盤面に置いた設備
  uid: string; fac: string;
  x: number; y: number; rot: 0|1|2|3;
  rec?: string|null;                 // 選んだレシピ
  item?: string|null;                // tap の場合、引き出す品目
}

interface Belt {                     // ベルト／パイプ 1本
  uid: string;
  from: string; fromPort: number;
  to:   string; toPort:   number;
  cells: {x:number;y:number}[];      // 経路。端の設備の口の正面マスから始まる
  item: string|null;
}

interface FieldEntry { fac: string; item: string; count: number; rate: number }  // 採取ゾーンの登録

interface Area {                     // 工業エリア1つ
  id: string; region: '四号谷地'|'武陵'; name: string;
  w: number; h: number;              // 盤面のマス数
  cap: number; zip: number; combat: number;   // 協約容量／ジップライン／戦闘設備の上限
  basePower?: number;
  goal?: {item:string; rate:number}|null;     // 検証目標
  field: FieldEntry[];
  nodes: Node[]; belts: Belt[];
  net: Record<string, number>;       // このエリアの倉庫への純増（他エリアの計算で使う）
}

interface Save {
  meta: { name:string; basePower:number; autoGrow?:boolean; pipeCap?:number; genRec?:string; goal?:... };
  items: Item[]; facs: Facility[]; recs: Recipe[];
  areas: Area[]; cur: number;
}
```

### 地域・エリアの関係（重要）

- **エリアごと**に独立: 盤面・採取ゾーン・協約容量・盤面サイズ。
- **地域ごと**に共通: 倉庫。四号谷地と武陵は完全に別勘定。
- 倉庫への入り = 同じ地域の**全エリアの採取量** + **他エリアの `net`**（最後にそのエリアを計算したときの純増）。
- 工業エリアの起点は必ず **倉庫搬出口**、終点は **倉庫搬入口／貯蔵設備**。
  採鉱機やポンプは工業エリアに置くものではない（フィールドの採取スポット）。

---

## 4. ゲーム定数と出典

| 値 | 内容 | 確度 |
|---|---|---|
| `BELT_CAP = 30` | ベルト1本 30個/分（1個/2秒） | **確認済** |
| `PIPE_CAP = 30` | パイプ1本の上限 | 要確認・設定で変更可 |
| `SPLIT_MAX = 3` | 分流器の分岐上限 | **確認済** |
| `GEN_SEC = 40` | 発電機がバッテリー1個を消費する秒数 | **確認済** |
| 協約容量 | 谷地通路100／中枢200／アブリー100／源石研究200／鉱山220／エネルギー高地200／武陵城350／景玉谷210／清波砦160／首礎180／実験区域150 | 攻略情報の一覧 |
| 天有洪炉 | 設置上限12台 | 生産ライン実例より |
| 武陵の採取上限 | 源石540／青鉄120／赤銅420／不活性ガス460／息壌ガス100（個/分） | Ver1.4 実測メモ |

レシピの比率は公開されている生産ライン実数から逆算したもの（`conf:'part'`）。
**1周期の秒数はライン本数からの推定を含む。** 確認済(`ok`)は4件だけ。
数値の大半は暫定なので、**ユーザーがゲーム内の製造プロセス画面を見て上書きする前提**の設計にすること。

---

## 5. 中核の4アルゴリズム

移植で最も注意が必要な部分。**それぞれ「なぜそうなっているか」を壊さないこと。**

### 5-1. ソルバー（solver.ts）

2段構えで収束させる。

1. **需要（desired）を下流から上流へ伝える。** 「作れる量」ではなく「引き取ってもらえる量」の上限を求める。
   出口にラインが無い設備は稼働率0（在庫が満杯になって止まる）。
2. **実際の流量（achieved）を収束させる。** 緩和係数 0.45、240反復。

守るべき性質:

- **分配は需要比例。** 同じ出口から複数のラインが出るとき、需要に応じて配分する。
  分流器は片側が詰まればもう片側へ流すので、等分ではない（背圧あり）。
- **電力の充足率は、収束後に一度だけ全体へ掛ける。**
  反復のたびに掛けると上流から下流へ段ごとに累乗され（3段で pf³）、実態の何分の一かの値になる。
  *これは実際に踏んだバグ。充足率55%の系で最終工程が1%と出ていた。*
- **倉庫搬出口の供給量**は `min(相ごとの上限, 倉庫への入り / その品目の搬出口数)`。
  倉庫の入りは同じ地域の採取量＋他エリアの純増＋このエリアの搬入口に届いた分。
- 発電機の燃料は**倉庫から引く**。生産ツリーに混ぜない（§5-3）。

### 5-2. 逆算プランナー（planner.ts）

目標産出量から必要台数を求める。

- 需要を再帰展開してレシピ別サイクル数を出し、`machines = cycles * sec / 60`、実台数は切り上げ。
- **副産物（`out[0]` 以外）は「狙って作るもの」の候補にしない。**
  これを入れないと、汚水ほしさに赤銅塊を、そのために水を、水のために汚水を…と無限に膨らむ。
  *実際に発散して台数が 1e263 になった。* 発散検知（`deficit > 1e9`）も残すこと。
- **採取ゾーンにある品目・他エリアが余らせている品目は「作らず倉庫から引く」。** ただし目標品目自身は除く。
- **端数最適化**: 台数は目標値に比例する分数になるので、連分数展開で分母を求め、
  全設備が整数台・100%稼働になる最小の目標値（の倍数）を提示する。
  例: 小容量バッテリーは30個/分の倍数なら無駄ゼロ。

### 5-3. 自動配置（layout/）

**1本のライン方式**と**ブロック方式**の2つ。

段の決め方・並べ方:

- 段は「**最終産物から何段手前か**」で決める。原料からの距離で決めると、
  終盤でしか使わない枝（蕎花のような素材）が左端に流されて長距離配線になる。
- 段内の順序は**重心法**（つながる相手の平均位置）で決め、段ごとの**縦の広がりを揃える**。
  台数の違う段（採鉱機9台と精錬炉5台）でも相手が真横に来る。
- 分流器・合流器は、繋がる相手の平均の高さへ寄せる。

**処理順序が決定的に重要**（実際に踏んだバグ）:

```
1. 段ごとのスロットを作る（採取口・生産設備・発電機とその燃料取出口）
2. スロット同士の接続を決める（貪欲マッチング）。余った産出量を surplus に記録
3. 【必ずここで】余った産出（最終産物も副産物も）に貯蔵設備を足す
   ← 分流器を挟む処理より後にやると、口の割り当て済みの設備に接続を足すことになり
     「搬出口が満杯」で配線が落ちる。失敗22本の原因がこれだった
4. 口の数を超える分岐・合流に分流器／合流器の均等木を挟む（品目をまたいで1本にまとめない）
5. 座標を割り当てる
6. 高さの近い組から配線し、落ちた線を先頭に回して全部引き直す（最大4巡）
```

**ブロック方式**: 全設備が整数台になる最小ユニットを1枚だけ丁寧に組み（置き方を最大27通り試し、
時間で打ち切る）、平行移動で敷き詰める。1枚が自己完結しているので何枚並べても配線が破綻しない。

### 5-4. 経路探索（routing.ts）

ダイクストラ法。**コストの設計がすべて。**

- 基本コスト 1/マス。
- **他の設備の口の正面マスは +5。** 塞ぐくらいなら少し遠回りするが、大回りするくらいなら1マス横切る。
  ここを絶対禁止にすると、盤面を一周する経路を選んで他を全部塞ぐ。
- **既存ラインとの交差は +12。** 直進しているラインには**直交方向なら跨げる**
  （ベルトブリッジ／パイプブリッジ／地下配管）。同じ向きに重ねること、曲がり角を跨ぐことは不可。
  交差箇所は盤面に表示し、件数を診断に出す。
- 遠回りの上限は**コストではなく通過マス数**で見る（`man*3+80`）。
  コストで打ち切ると、口の正面を何度か跨ぐ正当な経路が弾かれる。
- 口の組み合わせは近い順に12通りまで試し、**最も安い経路**を採る。

---

## 6. 移植の順序

1. **domain の型と定数**を切り出し、seed データを `data/seed.ts` へ。
2. **geometry と routing** を移植。単体テスト（直線・迂回・交差・口の正面回避）を書く。
3. **solver** を移植。§7 の回帰ケースで数値を突き合わせる。**ここが通るまで UI に触らない。**
4. **planner** を移植。端数最適化の分数演算にテストを書く。
5. **layout** を移植。処理順序（§5-3）をコメントで明示すること。
6. UI を組み直す。既存の画面構成（下記）は概ね妥当なので踏襲してよい。
7. 保存形式の後方互換を確認（現行の JSON がそのまま読めること）。

### 現行の画面構成

- ヘッダー: 電力バー／採取ゾーン／生産目標／データ編集／保存読込／使い方
- 地域タブ（四号谷地・武陵）＋エリアタブ＋協約容量の使用量表示
- 左: 設備パレット（採取設備は出さない）／中央: 盤面 canvas ／右: 検査パネル
- 下: 品目バランス／効率メトリクス／診断（**行クリックで該当箇所へジャンプ**）
- データ編集は 品目／設備／レシピ／まとめて入力／網羅状況 のタブ

### 「まとめて入力」の書式（維持すること）

```
設備名 秒数s: 材料*数 + 材料*数 -> 産物*数   #確 / #一部 / #推定
包装機 10s: 鉱物粉末*2 -> 小容量谷地バッテリー*1 #確
```

知らない品目名は自動で品目を作る。現在の内容を同じ書式で書き出せる（**往復してもレシピ数が変わらないこと**＝べき等性はテストで担保）。

---

## 7. 回帰ケース（移植の合否判定）

初期データ・初期採取ゾーンのまま、各エリアで目標を組み、自動配置してソルバーを回した結果。
**配線失敗0** と **搬入量** を再現できれば移植は成功とみなす。

| エリア | 目標 | 配線失敗 | 搬入/分 | 電力 |
|---|---|---|---|---|
| 中枢エリア | 結晶外殻 20 | 0 | 20.0 | 240/48 |
| 中枢エリア | 小容量谷地バッテリー 6 | 0 | 15.0 | 240/216 |
| 鉱山エリア | 紫晶製ボトル 15 | 0 | 12.0 | 240/48 |
| 首礎 | 赤銅塊 120 | 0 | 120.0 | 240/96 |
| 武陵城 | 赤銅塊 240 | 0 | 240.0 | 240/192 |
| 武陵城 | 小容量武陵バッテリー 30 | 0 | 26.6 | 240/216 |
| 応龍関 | 分離コア 60 | 0 | 60.0 | 240/80 |
| 天王原 | 息壌 120 | 0 | 56.2 | 240/512 |

**エリア連携のテスト**（この順に実行すること）:

1. 天王原で息壌120 → 2. 武陵城で小容量武陵バッテリー30 → 3. 応龍関で分離コア60
   → **搬入60.0（達成100%）／電力240/128** になる。
   燃料が無い状態で 3 だけ実行すると `発電機の燃料が足りません` と `搬入37.5` になる。

**逆算の検算**（出典の実数と一致すること）:

| 目標 | 出典の投入量 | 期待される算出 |
|---|---|---|
| 赤銅塊 240/分 | 赤銅鉱石240・水240 | 赤銅鉱物240／水240 |
| 焔銅部品 12/分 | 赤銅塊240・分離コア60・息壌ガス96 | 赤銅鉱物300／息壌ガス96 |
| 緋銅部品 3/分 | 赤銅塊60・分離コア15・息壌ガス12 | 赤銅鉱物75／息壌ガス12 |

**端数最適化**: 初期データで小容量谷地バッテリーの単位は **30個/分**（採鉱機8・精錬炉4・包装機5で整数台）。

---

## 8. 既知の未解決課題（再構成で直したい）

1. **多段構成で稼働率が揃わない。** 中容量武陵バッテリーのように6〜8段になると、
   貪欲マッチングの端数が偏って各設備が中途半端な稼働率になり、搬入量が目標に届かない。
   → 需要比例で割り当てる、または最小費用流として解くのが筋。
2. **応龍関の重息壌で配線が1本落ちる。** 他は0なので、経路探索の最後の詰め。
3. **ブロック方式の探索が重い。** 27通り×大きな計画で数秒かかる。
   計算を Web Worker に逃がすと UI が固まらない。
4. **ソルバーが同期実行。** 大きな盤面では体感が重い。Worker 化を検討。
5. **循環レシピ**（サンドリーフ⇄種）の扱いが素朴。反復上限で誤魔化している。
6. データの確度が `part`/`guess` のものが大半。**UI で「この計画は推定値に依存している」と示す**
   仕組みは入れてあるが、もっと前面に出してよい。

---

## 9. 用語（ゲーム内呼称に合わせること）

協約核心＝倉庫 / 倉庫搬出口・倉庫搬入口 / 協約貯蔵箱 / 集成工業エリア /
採取ゾーン（フィールド）/ 地域建設レベル / 協約容量 / 分流器・合流器 /
ベルトブリッジ・パイプブリッジ・地下配管 / 天有洪炉 / 息壌・重息壌 / 谷地取引券・武陵取引券
